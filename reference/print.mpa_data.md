# S3 print method for 'mpa_data'.

Prints a short representation of mpa_data returned by `generate_data`.

## Usage

``` r
# S3 method for class 'mpa_data'
print(mpa_data, ...)
```

## Arguments

- ...:

  Extra parameters for `print`

- x:

  An object of class `mpa_data` as returned by `generate_data`.
